SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# Optional From override (defaults to SMTP_USER)
SMTP_FROM=
# Destination inbox — never shown on public pages. Set in Railway Variables.
CONTACT_TO_EMAIL=
